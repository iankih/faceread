---
description: "모든 작업의 기본 가이드 – 항상 적용"
alwaysApply: true
---

# 🏁 00-MASTER-GUIDE.mdc  
**이 파일은 Cursor Agent가 _항상_ 먼저 읽어야 하는 최상위 규칙입니다.**  
모든 작업은 _반드시_ 아래 순서를 따라 추가 규칙을 참조‧적용합니다.

---

## 1 | 프로젝트 개요 & 공통 목표
| 항목 | 내용 |
| --- | --- |
| 제품 | **감정 인식 퀴즈 웹앱** (모바일 퍼스트) |
| 핵심 스택 | `React 18` + `TypeScript 5` + `Tailwind CSS` |
| 필수 라이브러리 | Radix UI · shadcn/ui · Framer Motion |
| 성능 목표 | **FCP < 1.5 s · LCP < 2.5 s** _(모바일 4G 기준)_ |
| 접근성 목표 | **WCAG 2.1 AA ≥ 95 점** |
| 보안 원칙 | 개인정보 수집 금지 · CSP 준수 · 모든 입력 검증 |

---

## 2 | 작업 유형별 **세부 규칙** 참조 순서
> **Cursor Agent는 작업 유형을 판단한 뒤, 아래 파일들을 순서대로 자동·수동 적용해야 합니다.**

| 작업 유형 | 자동 적용 (globs) | 필요 시 수동 호출 |
| --- | --- | --- |
| 🎨 UI 컴포넌트 | `ui-components.mdc` | `design-system.mdc` |
| 📊 데이터·상태 | `data-structure.mdc` | `performance.mdc` |
| 💻 코딩 표준 | (마스터에 포함) | — |
| 🌐 국제화 | — | `internationalization.mdc` |
| ⚡ 성능 최적화 | — | `performance.mdc` |
| 🔒 보안 | (항상 적용) | — |

_파일 위치: `.cursor/rules/`_

---

## 3 | 🚨 **모든 작업 공통 필수사항**
1. **TypeScript** `strict` 모드 준수  
2. ESLint & Prettier 오류 **0**  
3. Tailwind CSS 만 사용·_인라인 스타일 금지_  
4. 모바일 우선 **반응형** 디자인  
5. 성능·접근성 검수 체크리스트 통과  
6. **보안 가이드**(`security.mdc`) 위반 금지  

---

## 4 | 📝 작업 완료 전 체크리스트
- [ ] 관련 세부 규칙 파일 모두 검토 ✅  
- [ ] TypeScript 오류 없음 ✅  
- [ ] Lighthouse 접근성 ≥ 95 ✅  
- [ ] FCP/LCP 목표 충족 ✅  
- [ ] 보안·개인정보 정책 위배 없음 ✅  

---

## 5 | 프롬프트 예시
### 기본 컴포넌트 작업
```
@Cursor Rules
QuestionCard 컴포넌트를 구현해줘.
(마스터 → ui-components → 코딩 표준 순 적용)
```

### 복합 작업
```
@Cursor Rules
마스터 가이드부터 단계별 규칙을 적용해서:
1. 데이터 구조 설계
2. 상태 관리 구현
3. UI 컴포넌트 제작
4. 성능 최적화
```

---

> **⚠️ 중요:** 이 파일을 읽지 않고는 어떤 코드도 생성하지 마세요.  
> 필요 시 `@ruleName`으로 추가 규칙을 수동 호출해 세부 가이드를 적용하십시오.
